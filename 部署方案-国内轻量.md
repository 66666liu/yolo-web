# 夜鹭页录 · 国内轻量服务器部署方案

**推荐**：阿里云或腾讯云**香港节点**轻量应用服务器，**2 核 2G / 40G 盘**，约 **25~40 元/月**（新用户活动价；续费会涨到 288~360 元/年，到期换账号重购是常规操作）。

选香港的原因：**不需要 ICP 备案**，且到大陆延迟比新加坡低。如果愿意等 2~3 周备案、要国内 CDN 加速，再换国内节点。

---

## 一、改造清单

### ✅ 先说不用改的（这是 VPS 比 PaaS 省事的地方）

| 项目 | PaaS 上 | VPS 上 |
|---|---|---|
| `trustProxy` | **必须**改成 `true`，否则全站共用一个 IP，额度和限流全废 | **默认 `'loopback'` 就是对的** —— nginx 和 Node 同机，代理就在回环上，不用动 |
| 状态文件路径 | 硬编码在 `__dirname`，得加 `DATA_DIR` 环境变量配合挂载磁盘 | **不用动** —— 服务器磁盘天然持久，`__dirname` 本身就是持久目录 |
| 单实例约束 | 得靠"挂磁盘就不许开副本"来兜 | **天然满足**，一台机器跑一个进程 |
| 模型入库 | 必须 `git add -f models/yelu.onnx` 进仓库让构建能拿到 | **不用入库**，scp 传一次就行 |

### ⚠️ 必改的 5 项

#### 1. 把所有外部 CDN 换成本地资源（**最关键，不改页面直接裸奔**）

`public/index.html` 现在依赖三个外部域名：

| 位置 | 资源 | 后果 |
|---|---|---|
| `index.html:8` | `https://cdn.tailwindcss.com` | **整站样式**。国内访问不稳定经常被墙，挂了就是完全无样式的白页 |
| `index.html:10` | `https://cdn.jsdelivr.net/npm/font-awesome@4.7.0/...` | 所有图标消失（jsDelivr 在国内经常不可用） |
| `index.html:2877,2916` | `https://picsum.photos/seed/...` | 占位图裂掉 |

改法：

- **Font Awesome**：`public/font-awesome.min.css`（30KB）**已经在仓库里了**，改一行链接即可
- **picsum**：换成一张本地占位图，或删掉这两处（只是默认图和错误图）
- **Tailwind**：这是主要工作量。项目里 `npm run build` 脚本已经有了
  （`tailwindcss build src/input.css -o public/output.css`），但需要：

  1. 把 `index.html` 里 `<style type="text/tailwindcss">` 整块内容挪进 `src/input.css`
  2. 把 `index.html` 里的 `tailwind.config = {...}` 挪进 `tailwind.config.js`
     （配色已经一模一样，直接确认即可，不用重写）
  3. `index.html` 改成引 `<link href="output.css">`，删掉 CDN 和 `tailwind.config` 脚本块

  ⚠️ **风险点**：Play CDN 是在浏览器里实时编译的，能识别运行时才注入的类名；
  改成静态构建后，Tailwind 只能靠**扫源码文本**找类名。

  - 好在抽卡那套配色（`TIER_STYLE`、概率框的 `text-base md:text-2xl` 等）
    都是**完整的类名字面量**写在 `index.html` 的内联 `<script>` 里，
    而 `content: ["./public/**/*.html"]` 扫的就是这个文件，**能扫到** ✓
  - 但如果有任何地方是**拼接**出来的类名（`bg-${color}-500` 这种），静态构建会漏掉，
    那种地方要么改成完整字面量，要么在 `safelist` 里列出来

  这个改造有点细，**建议让我来做**，我可以顺便列一份"哪些类名是动态拼的"清单。

- 顺带：`public/output.css`（12KB）和 `public/tailwind.min.css`（824KB）现在是**死文件**，
  全项目没有任何地方引用。改成本地构建后 `output.css` 会复活，`tailwind.min.css` 可以删。

#### 2. `data.json` 已经在 git 里了 —— `git pull` 会覆盖用户数据

`data.json` 是**图鉴正文**，服务运行时会重写它，但它**同时被 git 跟踪**。
服务器上 `git pull` 更新代码时，会拿仓库里的版本覆盖线上数据 —— 用户上传的图全没。

改法（部署时执行一次）：

```bash
git update-index --skip-worktree data.json
```

这告诉 git"别再管这个文件的改动"。配合第六节的自动备份，风险就控住了。

> 更彻底的做法是把所有运行时状态移到一个 `data/` 目录并整体 gitignore 掉，
> 但那要改 server.js 里十几处路径，收益不大，不急。

#### 3. 装中文字体（不装的话长图名字全是方块）

全站长图的名字是**服务端用 sharp 渲染 SVG** 出来的，容器/服务器里没有中文字体就一片方块。

```bash
apt install -y fonts-noto-cjk
```

`longimage.js` 的字体回退链是 `Microsoft YaHei, SimHei, PingFang SC, Noto Sans CJK SC, sans-serif`，
Linux 上没有前三个，会落到 **Noto Sans CJK SC** —— 所以装这个包正好。

#### 4. `models/yelu.onnx` 和 `config.js` 不在仓库里，手动传

```bash
scp models/yelu.onnx root@服务器IP:/srv/yelu/models/
scp config.js        root@服务器IP:/srv/yelu/
```

`config.js` 里填 `adminKeyHash`（不要在公网服务器上放明文 `adminKey`）：

```bash
node -e "console.log(require('crypto').createHash('sha256').update('你的口令').digest('hex'))"
```

也顺便把 `corsOrigins` 改成你的真实域名（同源访问其实用不到，但留着 `natappfree.cc` 不体面）。

#### 5. 清掉硬编码的旧域名

`public/index.html:359`（导出区块里）：

```html
<p class="text-black ml-1 mt-2 mb-4 text-base export-only">来自：夜鹭页录 https://yeluyelu.mynatapp.cc</p>
```

那是 natapp 内网穿透的临时域名，换成你的正式域名。另外 `config.js` 里的 `corsOrigins`
也有一个 `natappfree.cc`，一并换掉。

### 💡 可选的

- **Live2D 看板娘**：`public/live2d/`（368KB 模型）被 gitignore，
  `public/live2d-vendor/`（152KB 框架）**已入库**。模型不传的话看板娘会走 CDN 兜底或回退静态立绘，
  页面上不显示。想要就 `scp -r public/live2d root@IP:/srv/yelu/public/`
- 顺手把打错的 remote 清掉：`git remote remove ortgin`

---

## 二、买服务器

1. 阿里云/腾讯云 → 轻量应用服务器 → **地域选"香港"**
2. 配置：**2 核 2G**，系统盘 40G 起，带宽 20~30M（够用；图片站流量别选太小）
3. 镜像：**Ubuntu 24.04 LTS**（纯净系统，别选带面板的，后面 apt 会有冲突）
4. 记下公网 IP，设置 root 密码或密钥

> **2G 内存是底线**：onnxruntime 加载模型 + sharp 合成 2300 万像素长图，1G 会 OOM。
> 如果只买 1G，务必加 swap（第三节有）。

域名：随便一个域名商买，DNS 加一条 A 记录指向服务器 IP。

---

## 三、服务器初始化（只做一次）

```bash
# ① 系统更新
apt update && apt upgrade -y

# ② Node.js 22 LTS
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs

# ③ 系统依赖：中文字体（长图必需）、sharp 依赖、nginx、pm2
apt install -y fonts-noto-cjk libvips-dev build-essential nginx
npm install -g pm2

# ④ 防火墙：只开 22 / 80 / 443
ufw allow 22 && ufw allow 80 && ufw allow 443 && ufw --force enable

# ⑤ 2G 内存建议加 swap（防止长图生成时 OOM）
fallocate -l 2G /swapfile && chmod 600 /swapfile
mkswap /swapfile && swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# ⑥ 建目录
mkdir -p /srv/yelu
```

---

## 四、部署代码

### 首次部署

```bash
cd /srv/yelu
git clone https://github.com/66666liu/yolo-web.git .

npm ci --omit=dev

# 从本机传两个 gitignore 的必需文件
# （在你自己电脑上执行）
#   scp models/yelu.onnx root@服务器IP:/srv/yelu/models/
#   scp config.js        root@服务器IP:/srv/yelu/

# 保护运行时数据，别被以后的 git pull 覆盖
git update-index --skip-worktree data.json

# 起服务
pm2 start server.js --name yelu
pm2 save
pm2 startup        # 按输出提示再执行它打印的那条命令，实现开机自启
```

看日志确认两个关键信号：

```bash
pm2 logs yelu --lines 50
# 期望看到：[longimage] 全站长图就绪：xxx 张，696×xxxxx
```

### ⚠️ 前端改造要先做完

第一节的 CDN 本地化**必须在构建前完成**，否则页面在服务器上是无样式的。

如果你走 Tailwind 本地构建那条路，`npm ci` 之后要跑一次：

```bash
npm run build      # 生成 public/output.css
```

`devDependencies` 里有 tailwindcss，所以**不能**用 `npm ci --omit=dev`；
改成 `npm ci` 装全量，或者把构建产物 `public/output.css` 直接提交进仓库（更省事，推荐）。

---

## 五、nginx 反代 + HTTPS

`/etc/nginx/sites-available/yelu`：

```nginx
server {
    listen 80;
    server_name 你的域名;

    # 图片上传上限，要和 config.js 的 maxImageMB 对得上
    client_max_body_size 25M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # 传给 Node 的真实 IP —— trustProxy: 'loopback' 靠这几个头工作
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 长图生成 / 图片检测可能慢，超时给足
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
```

```bash
ln -s /etc/nginx/sites-available/yelu /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx

# HTTPS（certbot 自动续期）
apt install -y certbot python3-certbot-nginx
certbot --nginx -d 你的域名
```

> `proxy_set_header X-Forwarded-For` 那几行别漏 —— 漏了的话 `req.ip` 会变成 `127.0.0.1`，
> 全站访问者又共用一个 IP，每日上传额度和限流会误伤所有人。

---

## 六、更新与备份

### 更新代码

```bash
cd /srv/yelu
git pull
npm ci --omit=dev          # 或 npm ci && npm run build（看第一节选的方案）
pm2 restart yelu
```

`git update-index --skip-worktree data.json` 已经设过，`git pull` 不会碰它。

### 备份（**务必配**）

用户上传的图和点赞/抽卡记录全在服务器上，丢了就没了。加个 crontab：

```bash
crontab -e
```

```cron
# 每天凌晨 4:10 备份数据与图片，保留 14 天
10 4 * * * cd /srv/yelu && tar czf /srv/backup/yelu-$(date +\%F).tgz data.json likes.json gacha_tickets.json public/images 2>/dev/null && find /srv/backup -name 'yelu-*.tgz' -mtime +14 -delete
```

```bash
mkdir -p /srv/backup
```

有对象存储的话，把 `/srv/backup` 再同步上去更好（服务器本身也可能整个挂掉）。

---

## 七、验收清单

- [ ] `curl https://你的域名/api/birds/count` 返回图鉴数量
- [ ] **页面样式正常**（说明 Tailwind 本地化成功了，没有偷偷走 CDN）
- [ ] **图标正常显示**（说明 Font Awesome 本地化成功了）
- [ ] 上传一张图，然后 `git pull && pm2 restart`，**确认图还在**（验证 `skip-worktree` 生效）
- [ ] 搜索框输入 `login 你的口令` 能进后台
- [ ] 抽到「终极」后下载全站长图，**确认名字是中文不是方块**（验证中文字体）
- [ ] 从手机 4G 访问一次，测下真实速度和移动端版式
- [ ] 限流没误伤：让另一个 IP（手机热点）也上传一张，确认两边额度独立
      （说明 `X-Forwarded-For` 透传正确）

---

## 价格参考

新用户活动价（2026-09 查到的，以官网实时报价为准）：

| 厂商 | 配置 | 活动价 | 续费 |
|---|---|---|---|
| 阿里云轻量 香港 | 2核2G / 30M / 1TB | 约 34 元/月 | 约 288 元/年 |
| 阿里云轻量 香港 | 2核2G / 40G SSD / 200M峰值 / 不限流量 | 约 39 元/月，年付可低至 68 元/年 | — |
| 腾讯云轻量 香港 | 2核2G / 30M / 1.2TB | 约 28 元/月 | 约 360 元/年 |
| 腾讯云轻量 香港 | 2核2G / 20M 固定带宽 | 约 40 元/月 / 408 元/年 | — |

来源：[阿里云开发者社区价格整理](https://developer.aliyun.com/article/1711946)、
[腾讯云香港服务器价格](https://www.cnzhuji.com/article/57_120963.html)、
[香港 VPS 选购指南](https://www.xoidc.com/vps/post/1453.html)
